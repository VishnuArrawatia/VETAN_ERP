node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local database and persisted data (must not be committed)
Payroll.db
Payroll.db-journal
payroll_persisted_store.json
payroll_persisted_store.prev.json

# Rotating safety snapshots (daily + pre-restore/pre-purge copies)
data-backups/

# Codebuff internal
.freebuff/
