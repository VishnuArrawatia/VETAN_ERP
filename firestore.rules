rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Secure by default: Lock down client-side access completely.
      // All operations are handled securely server-side using the Firebase Admin/Server SDK.
      allow read, write: if false;
    }
  }
}
